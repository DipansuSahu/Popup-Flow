# Popup-Flow
PopupFlow is a lightweight, reusable popup management system for Unity. It supports queue-based popups, flexible configuration (title, description, buttons, callbacks), DOTween animations, default fallbacks, and automatic EventSystem creation. Designed for clean architecture and easy reuse across projects.
